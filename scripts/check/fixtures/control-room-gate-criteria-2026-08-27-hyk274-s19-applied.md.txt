| S19 | **워커 좌석에 «대화형 메뉴」를 남긴 채 배달하지 않는다 · 배달 직후 «착수」를 1회 확인한다 (2026-08-16 한용 위임·책임자 확정 · 상설)** — ⛔**ⓐ 워커 좌석에서 검증용 대화형 명령(`/mcp`·`/usage` 등)을 쓴 뒤에는 «반드시 닫고 프롬프트 복귀를 화면으로 확인」한 뒤에만 배달한다**(배달 전 좌석 상태 = «메뉴 없음 + 입력줄 비어 있음»까지 본다). ⛔**ⓑ MCP 실측은 «별도 `-p` 세션의 도구 목록 조회」를 주 근거로 삼고 «좌석 메뉴는 쓰지 않는다»**(책임자 확정 — 좌석을 건드리지 않는 방식이 이미 있다). ★**ⓒ 배달 직후 «워커가 실제로 움직였는지」를 1회 확인한다**(완료 감시와 «별개» — 완료 감시는 «끝났는가»만 보고 «시작했는가»는 못 본다). ⛔**«화면으로»가 아니다**(HYK-274 · 2026-08-27 실측 반영) — `orca terminal read --screen`은 76초+ 미반영·입력줄 잔류 등으로 무한정 낡을 수 있어 이 확인의 단독 근거가 될 수 없다. 정본 방법 = **세션 기록 파일 총 바이트 수 증가**(`dispatch-start-confirm-cli.mjs` + `dispatch-start-size-core.mjs`, orca 호출 0)로 «시작함/시작 못 함/시작 후 멈춤» 3상태를 판정한다 — 배달 경로에 이미 결선되어 있다. **★근거 실사례(2026-08-16 실측)** — ORCH 가 MCP 차단을 «검증」하려고 워커 좌석에 `/mcp` 를 띄우고 **닫지 않은 채 배달**했고, 지시문이 **그 메뉴에 먹혀 유실**돼 라운드가 **시작조차 못 한 채 8분 방치**됐다(발견 = **책임자 눈** · 배달기는 `ALLOW`·`RECORDED`·`OK` 를 전부 찍었다). ⇒ ***검증 행위가 검증 대상을 정지시켰다.*** ⚠️**규칙의 사각** — 기존 상설은 *«ORCH 화면에 메뉴를 띄우지 마라»* 였고 **«워커 좌석에 메뉴를 남긴다»는 문면에 걸리지 않았다** | 배달 직전 좌석 화면이 «프롬프트 상태 + 입력줄 비어 있음»인지 · MCP 실측 근거가 «좌석 메뉴」가 아니라 «별도 세션 도구 목록」인지 · 배달 직후 착수 확인 1회를 **세션 기록 파일 크기 증가로**(화면이 아니라) 했는지 (⚠️**정직 한계**: 관제실 문서 규약이라 **기계 강제가 아니다** — 어겨도 아무 검사가 안 걸린다. 차단 실적 0 = **unproven**. 기계화 대상 = **HYK-271**(메뉴 잔류) · **HYK-272**(배달기가 제출을 확인하지 않음, ★HYK-274 실측: 이미 세션 로그 크기 기반으로 기계화됨) · **HYK-270**(한도 정지 미기상) ⇒ ★**셋은 «멈춘 것을 아무도 모른다」는 한 축**이며 **묶음 트랙으로 제안**한다(책임자 확정)) |
